import React, {FC} from "react";

import styles from './styles.module.css';

const Maps: FC = () => {

    return (
        <div className={styles.mapContainer}>
            Map here
        </div>

    )
}

export default Maps;